package com.santansarah.domain

//import com.santansarah.utils.AppErrorCodes
import com.santansarah.utils.ErrorCode

/**
 * Singleton - Error codes with a friendly message.
 */
/*object AppErrors {
    *//**
     * Possible User Errors
     *//*
    val invalidEmail = AppErrorCodes(ErrorCode.INVALID_EMAIL, "Invalid email.")
    val userExists = AppErrorCodes(ErrorCode.EMAIL_EXISTS, "This email address is already registered.")
    val unknownUser = AppErrorCodes(ErrorCode.UNKNOWN_USER, "This user doesn't exist.")

    *//**
     * Possible User/App Errors
     *//*
    val appExists = AppErrorCodes(ErrorCode.EMAIL_EXISTS, "This app already exists.")
    val invalidApp = AppErrorCodes(ErrorCode.INVALID_APP, "App name and app type can not be blank.")
    //val apiKeyExists = AppErrorCodes(ErrorCode.API_KEY, "There was a problem generating your API Key. Try again.")

    *//**
     * Global Errors
     *//*
    val databaseError = AppErrorCodes(ErrorCode.UNKNOWN, "Unknown database error. Try again, and check your parameters.")
}*/

